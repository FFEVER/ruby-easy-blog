<%= simple_form_for [ @article, @article.comments.build ] do |form|  %>
  <% if @comment&.errors&.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@comment&.errors&.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @comment&.errors&.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= form.input :commenter, label: 'Commenter', input_html: {value: current_user&.email} %>
  </p>
  <p>
    <%= form.input :body, as: :text, label: 'Body' %>
  </p>
  <p>
    <%= form.submit  %>
  </p>
<% end %>